# Task: Project Lookahead Mobile App — UI Fixes & Bug Fixes

## Context
This is a React Native / Expo mobile app for "Project Lookahead" — a construction project management tool. The web app just got UI fixes (font sizing, button spacing, alignment). Now the mobile app needs the same treatment.

## What to Fix
1. **Font consistency** — audit all text elements, ensure consistent type scale
2. **Button spacing** — tighten padding where excessive
3. **Alignment** — fix any misaligned elements
4. **Visual polish** — anything that looks "off"
5. **Bugs** — fix any broken layouts or visual issues

## Repo
`/Users/dave_patino/Desktop/ai/project-lookahead-mobile/`

## Steps
1. Read the main screens and components
2. Start the dev server: `npx expo start`
3. Identify all UI issues
4. Fix them systematically
5. Run `npx tsc --noEmit` to check for TypeScript errors
6. Commit with message: "fix(mobile): UI polish — fonts, spacing, alignment"

## What NOT to Change
- No backend/API changes
- No auth logic changes
- Don't remove existing functionality
